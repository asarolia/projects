@model DML.Generator.Web.Models.FileUploadViewModel
@using DML.Generator.Domain
@if (Model.sheetInformation != null && Model.sheetInformation.Count > 0)
{ 
    foreach (ExcelSheetInformation sheetInfo in Model.sheetInformation)
    {
        <div class="SheetInfo">
            <span class="cellContent">@sheetInfo.SheetName</span>
            <span class="cellContent">@sheetInfo.TableName</span>
            <span class="cellContent">@sheetInfo.HostDMLName</span>
            <span class="cellContent">@sheetInfo.FEDMlName</span>
        </div>
    }
}

